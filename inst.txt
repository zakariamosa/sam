ViT-B command:

mkdir -p models
curl -L https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth -o models/sam_vit_b_01ec64.pth

Restart server with:

export SAM_CHECKPOINT="models/sam_vit_b_01ec64.pth"
export SAM_MODEL_TYPE="vit_b"
export SAM_DEVICE="cpu"
export SAM_API_KEY="zakaria123"
uvicorn app:app --host 0.0.0.0 --port 8000

This should be much faster than ViT-H.
